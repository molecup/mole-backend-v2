'use strict';

/**
 * tournament-edition controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tournament-edition.tournament-edition');
